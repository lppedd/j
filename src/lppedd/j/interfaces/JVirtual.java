package lppedd.j.interfaces;

/**
 * A virtual object is an object that cannot live by itself, so its state cannot
 * be directly modified.
 *
 * @author Edoardo Luppi
 */
public interface JVirtual
{
    //
}
